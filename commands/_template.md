---
description: Replace with what this command does. Shown when Claude decides whether to surface it, and in the plugin/command listing.
disable-model-invocation: true
---

Replace this with the instructions Claude follows when this command is run.
Use `$ARGUMENTS` to capture whatever text the user types after the command
name.
